namespace MarsRover;

// Identical byte-for-byte across C#, TypeScript, and Python.
// The exception messages are the spec (see ../SCENARIOS.md).
public static class RoverMessages
{
    public const string UnknownCommand = "unknown command";
}

public static class GridDefaults
{
    public const int DefaultGridWidth = 100;
    public const int DefaultGridHeight = 100;
}
