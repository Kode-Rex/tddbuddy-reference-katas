namespace StringTransformer;

// Byte-identical across C#, TypeScript, and Python. The truncation
// marker is part of the spec (see ../SCENARIOS.md).
public static class TruncationMarker
{
    public const string Value = "...";
}
