namespace CodeBreaker;

// Named so the "4" appears once across the codebase. See the walkthrough for why
// F2 prefers a named constant over an inline literal.
public static class CodeLength
{
    public const int Pegs = 4;
}
